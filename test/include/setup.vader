Execute (Clean up test environment):
  let project_root = system("echo -en $(git rev-parse --show-toplevel || echo '.')")
  exec "source " . project_root . "/plugin/parinfer.vim"
  call SetupParinfer()

Before:
After:
Given:
